/* VENTARA POS — Pedidos. Módulo aislado: #orders + window.db.orders. */
(()=>{'use strict';
const S=['Pendiente','Entregado','Cancelado'],q=s=>document.querySelector(s),root=()=>q('#orders');
const appActive=()=>{const login=document.getElementById('loginScreen'),main=document.getElementById('mainApp');return !!main&&main.style.display!=='none'&&(!login||login.style.display==='none')};
function applyImmediateOrdersPatch(){
  if(!appActive())return;
  const scope=document.querySelector('#orders');
  if(!scope)return;
  const buscadorDuplicado=scope.querySelectorAll('.orders-search-filter, .filter-bar-container, div[class*="search"]');
  if(buscadorDuplicado.length>1)buscadorDuplicado[1].style.display='none';
}
const getDb=()=>{try{if(typeof db!=='undefined'&&db&&typeof db==='object')return db}catch(e){}try{if(typeof window.db==='object'&&window.db)return window.db}catch(e){}return null};
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const money=v=>{try{return typeof window.money==='function'?window.money(Number(v)||0):'$ '+Number(v||0).toLocaleString('es-CO')}catch{return '$ '+Number(v||0).toLocaleString('es-CO')}};
const products=()=>{const d=getDb();return Array.isArray(d?.products)?d.products:[]};
const customers=()=>{const d=getDb();return Array.isArray(d?.customers)?d.customers:Array.isArray(d?.clients)?d.clients:[]};
const orders=()=>{const d=getDb();if(!d)return[];d.orders=Array.isArray(d.orders)?d.orders:[];return d.orders};
const pn=p=>String(p?.name||p?.nombre||p?.description||p?.descripcion||'Producto');
const pp=p=>Number(p?.price??p?.salePrice??p?.sellingPrice??p?.precioVenta??p?.precio??p?.unitPrice??0)||0;
const cn=c=>String(c?.name||c?.fullName||c?.nombre||c?.businessName||c?.razonSocial||c?.commercialName||'Cliente');
const cp=c=>String(c?.phone||c?.telephone||c?.telefono||c?.mobile||c?.celular||c?.phoneNumber||'');
const ca=c=>String(c?.address||c?.direccion||c?.deliveryAddress||'');
const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const date=v=>{const a=String(v||'').split('-');return a.length===3?(`${a[2]}/${a[1]}/${a[0]}`):String(v||'—')};
const save=async()=>{const d=getDb();if(!d)return false;try{localStorage.setItem('ventara_pos_v1',JSON.stringify(d))}catch(e){console.warn('[VENTARA] orders local save',e)}try{if(typeof window.cloudSave==='function')await window.cloudSave()}catch(e){console.warn('[VENTARA] orders cloud save',e)}return true};
const toast=m=>{try{if(typeof window.toast==='function')window.toast(m);else console.log('[VENTARA]',m)}catch{}};
let draft=[],editingId=null,filters={text:'',status:'Todos',alert:'Todos'};
const normalize=s=>S.includes(String(s))?String(s):'Pendiente';
const alertInfo=o=>{const st=normalize(o?.status);if(st==='Entregado')return ['ENTREGADO','g'];if(st==='Cancelado')return ['CANCELADO','r'];const d=String(orderDate(o)||'');if(!/^\d{4}-\d{2}-\d{2}$/.test(d))return ['SIN FECHA','m'];const t=today();if(d<t)return ['¡VENCIDO / RETRASADO!','r'];if(d>t)return ['PROGRAMADO','b'];const tm=String(o?.deliveryTime||'');if(/^\d{2}:\d{2}$/.test(tm)){const now=new Date(),cur=`${String(now.getHours()).padStart(2,'0')}:${String(now.getMinutes()).padStart(2,'0')}`;if(cur>tm)return ['¡VENCIDO / RETRASADO!','r'];}return ['¡ENTREGAR HOY!','w']};
const badge=(v,c)=>`<span class="badge ${c}">${esc(v)}</span>`;
const total=o=>Number.isFinite(Number(o?.total))?Number(o.total)||0:(Array.isArray(o?.items)?o.items:[]).reduce((a,i)=>a+(Number(i?.qty)||0)*(Number(i?.price)||0),0);
const customerFor=o=>{const a=customers();const cid=o?.customerId??o?.clientId??'';return a.find(c=>String(c?.id)===String(cid))||a.find(c=>cn(c)===String(o?.customer||''))||a[0]};
const orderId=o=>String(o?.id||o?.number||'');
const orderDate=o=>o?.deliveryDate||o?.date||'';
const matches=o=>{const text=filters.text.trim().toLowerCase();const c=customerFor(o)||{};const hay=[orderId(o),o?.number,o?.orderNumber,o?.invoice,o?.invoiceNumber,o?.factura,o?.reference,o?.referencia,o?.ref,o?.customer,o?.customerId,o?.clientId,o?.phone,o?.address,cn(c),cp(c),c?.document,c?.documento,c?.cedula,c?.identification,c?.identificacion,c?.nit,c?.uniqueCode,c?.codigoUnico].map(x=>String(x??'').toLowerCase()).join(' ');if(text&&!hay.includes(text))return false;if(filters.status!=='Todos'&&normalize(o.status)!==filters.status)return false;if(filters.alert!=='Todos'&&alertInfo(o)[0]!==filters.alert)return false;return true};
const statusOptions=s=>S.map(x=>`<option value="${x}" ${x===normalize(s)?'selected':''}>${x}</option>`).join('');
function history(o,action){o.history=Array.isArray(o.history)?o.history:[];o.history.push({action,at:new Date().toISOString()})}
function row(o){const [al,ac]=alertInfo(o),c=customerFor(o),st=normalize(o.status);return `<tr><td><b>${esc(orderId(o)||'—')}</b></td><td>${esc(o.customer||cn(c)||'Consumidor Final')}</td><td style="white-space:normal">${esc(o.address||'—')}<br><small class="muted">${esc(o.phone||cp(c)||'Sin teléfono')}</small></td><td>${esc([date(orderDate(o)),o.deliveryTime].filter(x=>x&&x!=='—').join(' · ')||'Sin programar')}</td><td><b>${money(total(o))}</b></td><td><select class="order-status-select" data-id="${esc(o.id)}" aria-label="Estado del pedido ${esc(o.id)}">${statusOptions(st)}</select></td><td>${badge(al,ac)}</td><td><div class="actions" style="display:flex;gap:6px;flex-wrap:wrap"><button type="button" class="btn sm" data-action="details" data-id="${esc(o.id)}">Ver detalles</button>${st==='Pendiente'?'<button type="button" class="btn sm success" data-action="deliver" data-id="'+esc(o.id)+'">Marcar entregado</button><button type="button" class="btn sm danger" data-action="cancel" data-id="'+esc(o.id)+'">Cancelar</button>':''}</div></td></tr>`}
function removeDuplicateFilter(){const r=root();if(!r)return;const selectors=['.orders-search-filter','.filter-bar-container','.orders-filter-bar','.order-filters','.search-filter','.filters-bar'];const seen=[];selectors.forEach(sel=>Array.from(r.querySelectorAll(sel)).forEach(el=>{if(!seen.includes(el))seen.push(el)}));if(seen.length>1)seen.slice(1).forEach(el=>el.remove());Array.from(r.querySelectorAll('label')).filter(x=>x.textContent.trim()==='Buscar').forEach(label=>{const box=label.closest('.field')?.parentElement||label.parentElement?.parentElement;if(box&&box!==r&&!box.querySelector('#ventaraOrderFilterText')&&box.querySelector('select'))box.remove()});Array.from(r.querySelectorAll('input[type="search"]')).filter(x=>x.id!=='ventaraOrderFilterText').forEach(input=>{const box=input.closest('.field')?.parentElement||input.parentElement?.parentElement;if(box&&box!==r)box.remove()})}
function render(){const r=root(),all=orders();if(!r)return false;const list=all.filter(matches),t=today(),pending=all.filter(o=>normalize(o.status)==='Pendiente'),todayN=pending.filter(o=>orderDate(o)===t).length,overdue=pending.filter(o=>orderDate(o)&&orderDate(o)<t).length,scheduled=pending.filter(o=>orderDate(o)&&orderDate(o)>t).length,delivered=all.filter(o=>normalize(o.status)==='Entregado').length,cancelled=all.filter(o=>normalize(o.status)==='Cancelado').length;
r.innerHTML=`<div class="head"><div><h1>Pedidos</h1><p>Despacho, entregas programadas y seguimiento preventivo.</p></div><div class="actions"><button id="ventaraNewOrder" class="btn primary" type="button">+ Nuevo pedido</button></div></div>
<div class="card ventara-orders-card"><div class="ventara-orders-summary" style="display:flex;gap:16px;flex-wrap:wrap;margin-bottom:12px"><span><b>${pending.length}</b> pendientes</span><span><b>${todayN}</b> entregar hoy</span><span><b>${overdue}</b> retrasados</span><span><b>${scheduled}</b> programados</span><span><b>${delivered}</b> entregados</span><span><b>${cancelled}</b> cancelados</span></div>
<div class="ventara-orders-filters" style="display:flex;gap:10px;flex-wrap:wrap;align-items:end;margin:14px 0"><div class="field" style="flex:1;min-width:220px"><label>Buscar pedido</label><input id="ventaraOrderFilterText" type="search" value="${esc(filters.text)}" placeholder="Pedido, factura, referencia, cédula, único o cliente"></div><div class="field"><label>Estado</label><select id="ventaraOrderFilterStatus"><option>Todos</option>${S.map(s=>`<option ${filters.status===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="field"><label>Alerta</label><select id="ventaraOrderFilterAlert"><option>Todos</option><option ${filters.alert==='¡ENTREGAR HOY!'?'selected':''}>¡ENTREGAR HOY!</option><option ${filters.alert==='¡VENCIDO / RETRASADO!'?'selected':''}>¡VENCIDO / RETRASADO!</option><option ${filters.alert==='PROGRAMADO'?'selected':''}>PROGRAMADO</option><option ${filters.alert==='ENTREGADO'?'selected':''}>ENTREGADO</option><option ${filters.alert==='CANCELADO'?'selected':''}>CANCELADO</option><option ${filters.alert==='SIN FECHA'?'selected':''}>SIN FECHA</option></select></div><button type="button" class="btn" id="ventaraClearOrderFilters">Limpiar filtros</button></div>
<div class="tablewrap"><table class="table"><thead><tr><th>ID PEDIDO</th><th>CLIENTE</th><th>DIRECCIÓN / TEL.</th><th>FECHA / HORA ENTREGA</th><th>TOTAL</th><th>ESTADO</th><th>ALERTA</th><th>ACCIONES</th></tr></thead><tbody>${list.length?list.map(row).join(''):'<tr><td colspan="8" class="empty">No hay pedidos que coincidan con los filtros.</td></tr>'}</tbody></table></div></div>`;
bindList();removeDuplicateFilter();return true}
function bindList(){const r=root();r.querySelector('#ventaraNewOrder')?.addEventListener('click',()=>openModal());r.querySelector('#ventaraOrderFilterText')?.addEventListener('input',e=>{filters.text=e.target.value;render()});r.querySelector('#ventaraOrderFilterStatus')?.addEventListener('change',e=>{filters.status=e.target.value;render()});r.querySelector('#ventaraOrderFilterAlert')?.addEventListener('change',e=>{filters.alert=e.target.value;render()});r.querySelector('#ventaraClearOrderFilters')?.addEventListener('click',()=>{filters={text:'',status:'Todos',alert:'Todos'};render()});r.querySelectorAll('.order-status-select').forEach(s=>s.addEventListener('change',()=>setStatus(s.dataset.id,s.value)));r.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>{const o=orders().find(x=>String(x.id)===String(b.dataset.id));if(!o)return;b.dataset.action==='details'?details(o):b.dataset.action==='deliver'?setStatus(o.id,'Entregado'):b.dataset.action==='cancel'&&confirm(`¿Cancelar el pedido ${o.id}?`)&&setStatus(o.id,'Cancelado')}))}
function frame(id,title,html){document.getElementById(id)?.remove();const e=document.createElement('div');e.id=id;e.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,.45);display:flex;align-items:center;justify-content:center;z-index:100000';e.innerHTML=`<div style="width:min(980px,94vw);max-height:92vh;overflow:auto;background:#fff;border-radius:14px;padding:22px"><div style="display:flex;justify-content:space-between;align-items:center"><h2>${title}</h2><button class="btn sm" id="${id}Close">✕</button></div>${html}</div>`;document.body.appendChild(e);q('#'+id+'Close').onclick=()=>e.remove();return e}
function openModal(order=null){const d=getDb();if(!d)return toast('Los datos todavía no están disponibles.');editingId=order?.id||null;draft=(order?.items||[]).map(i=>({productId:i.productId,name:i.name||'Producto',qty:Number(i.qty)||1,price:Number(i.price)||0,subtotal:Number(i.subtotal)||((Number(i.qty)||0)*(Number(i.price)||0))}));const c=customerFor(order||{}),ps=products().filter(p=>p.active!==false).map(p=>`<option value="${esc(p.id)}">${esc(pn(p))} — ${money(pp(p))}</option>`).join(''),cs=customers().map(c=>`<option value="${esc(c.id)}" ${String(c.id)===String(order?.customerId||'')?'selected':''}>${esc(cn(c))}${cp(c)?' · '+esc(cp(c)):''}</option>`).join('');const e=frame('ventaraOrderModal',order?'Editar pedido '+order.id:'Nuevo pedido',`<form id="ventaraOrderForm"><div class="form"><div class="field"><label>Cliente</label><select id="oCustomer"><option value="">Consumidor Final</option>${cs}</select></div><div class="field"><label>Teléfono</label><input id="oPhone" value="${esc(order?.phone||cp(c))}"></div><div class="field full"><label>Dirección de entrega</label><input id="oAddress" value="${esc(order?.address||ca(c))}"></div><div class="field"><label>Fecha de entrega</label><input id="oDate" type="date" value="${esc(order?.deliveryDate||today())}"></div><div class="field"><label>Hora de entrega</label><input id="oTime" type="time" value="${esc(order?.deliveryTime||'')}"></div><div class="field"><label>Estado</label><select id="oStatus">${statusOptions(order?.status||'Pendiente')}</select></div></div><h3>Productos del pedido</h3><div style="display:grid;grid-template-columns:minmax(0,1fr) 120px auto;gap:10px;align-items:end"><div class="field"><label>Producto</label><select id="oProduct"><option value="">Selecciona un producto...</option>${ps}</select></div><div class="field"><label>Cantidad</label><input id="oQty" type="number" min="1" step="1" value="1"></div><button id="oAdd" type="button" class="btn primary">+ Agregar ítem</button></div><div class="tablewrap"><table class="table"><thead><tr><th>Producto</th><th>Cantidad</th><th>Precio Unit.</th><th>Subtotal</th><th>Eliminar</th></tr></thead><tbody id="oItems"></tbody></table></div><div class="totalline grand"><span>Total</span><b id="oTotal"></b></div><div class="actions" style="margin-top:16px"><button id="oCancel" type="button" class="btn">Cancelar</button><button class="btn success">Guardar pedido</button></div></form>`);q('#oCustomer').onchange=()=>{const x=customers().find(z=>String(z.id)===q('#oCustomer').value);if(x){q('#oPhone').value=cp(x);q('#oAddress').value=ca(x)}};q('#oAdd').onclick=add;q('#oCancel').onclick=()=>e.remove();q('#ventaraOrderForm').onsubmit=submit;items()}
function add(){const p=products().find(x=>String(x.id)===q('#oProduct').value),qty=Math.max(1,Math.floor(Number(q('#oQty').value)||1));if(!p)return toast('Selecciona un producto.');const x=draft.find(i=>String(i.productId)===String(p.id));if(x)x.qty+=qty;else draft.push({productId:p.id,name:pn(p),qty,price:pp(p)});draft.forEach(x=>x.subtotal=x.qty*x.price);items()}
function items(){const b=q('#oItems');if(!b)return;b.innerHTML=draft.length?draft.map((x,i)=>`<tr><td>${esc(x.name)}</td><td><input class="order-qty" data-i="${i}" type="number" min="1" step="1" value="${x.qty}" style="width:90px"></td><td>${money(x.price)}</td><td><b>${money(x.qty*x.price)}</b></td><td><button type="button" class="btn sm danger" data-del="${i}">Eliminar</button></td></tr>`).join(''):'<tr><td colspan="5" class="empty">Agrega productos al pedido.</td></tr>';b.querySelectorAll('[data-del]').forEach(x=>x.onclick=()=>{draft.splice(Number(x.dataset.del),1);items()});b.querySelectorAll('.order-qty').forEach(x=>x.onchange=()=>{draft[Number(x.dataset.i)].qty=Math.max(1,Math.floor(Number(x.value)||1));items()});q('#oTotal').textContent=money(draft.reduce((a,x)=>a+x.qty*x.price,0))}
async function closeOrderModal(e){if(!e)return;try{if(window.bootstrap?.Modal){const modal=bootstrap.Modal.getInstance(e)||bootstrap.Modal.getOrCreateInstance(e);modal.hide()}}catch(err){console.warn('[VENTARA] bootstrap modal close',err)}e.remove()}async function submit(e){e.preventDefault();if(!draft.length)return toast('Agrega al menos un producto al pedido.');const d=getDb();if(!d)return toast('Los datos todavía no están disponibles.');const c=customers().find(x=>String(x.id)===q('#oCustomer').value),now=new Date().toISOString(),payload={customer:c?cn(c):'Consumidor Final',customerId:c?.id||null,address:q('#oAddress').value.trim(),phone:q('#oPhone').value.trim(),deliveryDate:q('#oDate').value,deliveryTime:q('#oTime').value,items:draft.map(x=>({...x,subtotal:x.qty*x.price})),total:draft.reduce((a,x)=>a+x.qty*x.price,0),status:normalize(q('#oStatus').value)};if(editingId){const o=d.orders.find(x=>String(x.id)===String(editingId));if(!o)return toast('No se encontró el pedido.');Object.assign(o,payload,{updatedAt:now});history(o,'Pedido actualizado');await save();editingId=null;await closeOrderModal(e);render();toast('Pedido actualizado correctamente.');return}const o={id:'PED-'+Date.now().toString().slice(-5),...payload,createdAt:now,history:[{action:'Creado',at:now}]};d.orders.unshift(o);await save();editingId=null;await closeOrderModal(e);render();toast('Pedido creado correctamente.')}
async function setStatus(id,status){const d=getDb(),o=d?.orders?.find(x=>String(x.id)===String(id));if(!o||!S.includes(status))return;o.status=status;o.updatedAt=new Date().toISOString();if(status==='Entregado')o.deliveredAt=o.updatedAt;if(status==='Cancelado')o.cancelledAt=o.updatedAt;history(o,'Estado: '+status);await save();render();toast('Pedido '+o.id+': '+status+'.')}
function details(o){const items=(o.items||[]).map(x=>`<tr><td>${esc(x.name)}</td><td>${x.qty}</td><td>${money(x.price)}</td><td>${money(x.qty*x.price)}</td></tr>`).join('');frame('ventaraOrderDetails','Detalle del pedido',`<div class="form"><div class="field"><label>ID PEDIDO</label><input value="${esc(o.id)}" disabled></div><div class="field"><label>CLIENTE</label><input value="${esc(o.customer||'Consumidor Final')}" disabled></div><div class="field"><label>TELÉFONO</label><input value="${esc(o.phone||'—')}" disabled></div><div class="field full"><label>DIRECCIÓN</label><input value="${esc(o.address||'—')}" disabled></div><div class="field"><label>FECHA</label><input value="${esc(date(o.deliveryDate))}" disabled></div><div class="field"><label>HORA</label><input value="${esc(o.deliveryTime||'—')}" disabled></div><div class="field"><label>ESTADO</label><select id="detailStatus">${statusOptions(o.status)}</select></div></div><div class="tablewrap"><table class="table"><thead><tr><th>Producto</th><th>Cantidad</th><th>Precio Unit.</th><th>Subtotal</th></tr></thead><tbody>${items||'<tr><td colspan="4">Sin ítems</td></tr>'}</tbody></table></div><div class="totalline grand"><span>Total</span><b>${money(total(o))}</b></div><div class="actions" style="margin-top:16px"><button type="button" class="btn" id="detailClose">Cerrar</button><button type="button" class="btn primary" id="detailEdit">Editar</button></div>`);q('#detailStatus').onchange=()=>{setStatus(o.id,q('#detailStatus').value);document.getElementById('ventaraOrderDetails')?.remove()};q('#detailClose').onclick=()=>document.getElementById('ventaraOrderDetails')?.remove();q('#detailEdit').onclick=()=>{document.getElementById('ventaraOrderDetails')?.remove();openModal(o)}}
function mount(){const r=root();if(!r)return false;if(!r.querySelector('.ventara-orders-card'))return render();return true}
function watch(){let scheduled=false;const tick=()=>{if(!appActive())return;if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;if(!appActive())return;const r=root();if(r&&!r.querySelector('.ventara-orders-card'))render();else removeDuplicateFilter()})};const host=document.getElementById('mainApp');if(!host)return;new MutationObserver(tick).observe(host,{childList:true,subtree:true});tick()}
window.ventaraOrders={render,openNew:()=>openModal(),setStatus};window.renderOrders=render;window.openOrderModal=()=>openModal();window.setOrderStatus=setStatus;
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',watch,{once:true});else watch();
})();