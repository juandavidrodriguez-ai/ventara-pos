name: Apply surgical billing redesign (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete billing auto-patch disabled; billing is maintained directly in index.html."
